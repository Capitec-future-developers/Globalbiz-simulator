let body = document.querySelector('body');let chatInput = document.querySelector('.chat-input textarea');let sendChatBtn = document.querySelector('.chat-input span');let chatbox = document.querySelector('.chatbox');let chatbotToggler = document.querySelector('.chatbot-toggler');let closeBtn = document.querySelector('.chatbot header span');let userMessage;(function ensureChatbot() {    try {        const hasIcons = Array.from(document.querySelectorAll('link[rel="stylesheet"], link[rel="preload"]')).some(l => (l.href || '').includes('fonts.googleapis.com/icon?family=Material+Icons+Sharp'));        if (!hasIcons) {            const link = document.createElement('link');            link.rel = 'stylesheet';            link.href = 'https://fonts.googleapis.com/icon?family=Material+Icons+Sharp';            document.head.appendChild(link);        }        const addStylesheetOnce = (href) => {            if (!href) return;            const exists = Array.from(document.querySelectorAll('link[rel="stylesheet"]')).some(l => (l.getAttribute('href') || '').endsWith(href) || (l.href || '').includes(href));            if (!exists) {                const link = document.createElement('link');                link.rel = 'stylesheet';                link.href = href;                document.head.appendChild(link);            }        };        const path = (location.pathname || '').toLowerCase();        const isComputerDir = path.includes('/computer/') || path.includes('\\computer\\');        const isHomePage = path.includes('home page') || path.includes('home%20page');        const isComputer = isComputerDir && !isHomePage;        const isApp = path.includes('/app/') || path.includes('\\app\\');        const stylesBase = (isComputerDir || isApp) ? '../styles/' : 'styles/';        addStylesheetOnce(isComputer ? `${stylesBase}Chatbotcomputer.css` : `${stylesBase}chatbot.css`);        if (!document.querySelector('.chatbot')) {            const toggler = document.createElement('button');            toggler.className = 'chatbot-toggler';            toggler.innerHTML = '<span class="material-icons-sharp">mode_comment<\/span><span class="material-icons-sharp">close<\/span>';            const wrapper = document.createElement('div');            wrapper.className = 'chatbot';            wrapper.innerHTML = [                '<header>',                '  <h2>BIZCHAT<\/h2>',                '  <span class="material-icons-sharp">close<\/span>',                '<\/header>',                '<ul class="chatbox">',                '  <li class="chat incoming">',                '    <span class="material-icons-sharp">account_circle<\/span>',                '    <p>Welcome to Bizchat, how can I assist.<\/p>',                '  <\/li>',                '<\/ul>',                '<div class="chat-input">',                '  <textarea placeholder="Type your message here..." required><\/textarea>',                '  <span id="send-btn" class="material-icons-sharp">send<\/span>',                '<\/div>'            ].join('');            document.body.appendChild(toggler);            document.body.appendChild(wrapper);        }        body = document.body;        chatInput = document.querySelector('.chat-input textarea');        sendChatBtn = document.querySelector('.chat-input span');        chatbox = document.querySelector('.chatbox');        chatbotToggler = document.querySelector('.chatbot-toggler');        closeBtn = document.querySelector('.chatbot header span');    } catch (e) {        console.error('Failed to ensure chatbot presence:', e);    }})();function addWelcomeQuickReplies() {    if (!chatbox) return;    if (chatbox.querySelector('.chat-quick-replies')) return;    var quickReplies = [        { label: 'Pay a beneficiary', value: 'pay saved beneficiary' },        { label: 'Transfer money', value: 'open transfers' },        { label: 'Card help', value: 'card' },        { label: 'Documents', value: 'documents' },        { label: 'Fees', value: 'fees' },        { label: 'Contact support', value: 'contact support' }    ];    var chipsHtml = quickReplies.map(function (c) {        return '<button class="chip" data-chip="' + c.value + '">' + c.label + '</button>';    }).join('');    var li = document.createElement('li');    li.className = 'chat incoming chat-quick-replies';    li.innerHTML = '<span class="material-icons-sharp">account_circle</span><div class="incoming"><div class="chat-subtitle">Here\'s what I can help with:</div><div class="chat-chips">' + chipsHtml + '</div></div>';    chatbox.appendChild(li);}addWelcomeQuickReplies();const createChatLi = (message, className) => {    const chatLi = document.createElement('li');    chatLi.classList.add('chat', className);    let contentHtml;    if (className === 'outgoing') {        contentHtml = `<p>${message}</p>`;    } else {        contentHtml = `<span class="material-icons-sharp">account_circle</span><p class="incoming">${message}</p>`;    }    chatLi.innerHTML = contentHtml;    return chatLi;};const createIncomingHtml = (html) => {    const chatLi = document.createElement('li');    chatLi.classList.add('chat', 'incoming');    chatLi.innerHTML = `<span class="material-icons-sharp">account_circle</span><div class="incoming">${html}</div>`;    return chatLi;};const buildCard = ({ title, subtitle = '', body = '', list = [], links = [], chips = [] }) => {    const listHtml = list.length ? `<ul class="chat-list">${list.map(item => `<li>${item}</li>`).join('')}</ul>` : '';    const linksHtml = links.length ? `<div class="chat-links">${links.map(l => `<a class="chat-link" href="${l.href}" target="_blank" rel="noopener">${l.text}</a>`).join('')}</div>` : '';    const chipsHtml = chips.length ? `<div class="chat-chips">${chips.map(c => `<button class="chip" data-chip="${c.value}">${c.label}</button>`).join('')}</div>` : '';    const subtitleHtml = subtitle ? `<div class="chat-subtitle">${subtitle}</div>` : '';    return `      <div class="chat-card">        <div class="chat-title">${title}</div>        ${subtitleHtml}        ${body ? `<div class="chat-body">${body}</div>` : ''}        ${listHtml}        ${linksHtml}        ${chipsHtml}      </div>    `;};const getCurrentAccountInfo = () => {    const nameEl = document.getElementById('account-name');    const balEl = document.getElementById('account-balance');    return {        name: nameEl ? nameEl.textContent.trim() : 'Account',        balance: balEl ? balEl.textContent.trim() : 'N/A'    };};const buildPaySavedBeneficiaryHelp = () => {    const steps = [        "Tap Transact at the bottom.",        "Select Payments.",        "Choose Pay Saved beneficiary.",        "Pick a beneficiary from the list.",        "Enter amount and reference, then confirm."    ];    const stepsHtml = `<ol>${steps.map(s => `<li>${s}</li>`).join('')}</ol>`;    const html = `        <div>            <p>Here are the steps to pay a saved beneficiary:</p>            ${stepsHtml}            <div style="margin-top:8px;display:flex;gap:8px;flex-wrap:wrap;">                <button id="btn-start-pay-saved" style="padding:6px 10px;background:#007fff;color:#fff;border:none;border-radius:4px;cursor:pointer;">Start automation</button>                <button id="btn-show-where" style="padding:6px 10px;background:#f0f0f0;color:#333;border:none;border-radius:4px;cursor:pointer;">Highlight steps</button>            </div>            <p style="font-size:12px;color:#666;margin-top:6px;">Automation will try to click through the UI for you. You can pause or terminate anytime.</p>        </div>    `;    return html;};const tryExecuteAutomation = (command) => {    try {        if (typeof executeCommand === 'function') {            executeCommand(command);        } else {            const input = document.getElementById('automation-search');            const btn = document.getElementById('execute-automation');            if (input && btn) {                input.value = command;                btn.click();            } else {                alert('Automation controls not available on this page.');            }        }    } catch (e) {        console.error('Failed to start automation:', e);        alert('Unable to start automation.');    }};const generateResponse = (text) => {    const msg = (text || '').toLowerCase();    if (/(how.*pay.*saved.*beneficiar|pay saved beneficiar|saved beneficiar.*pay)/i.test(msg)) {        const html = buildPaySavedBeneficiaryHelp();        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        const startBtn = li.querySelector('#btn-start-pay-saved');        const highlightBtn = li.querySelector('#btn-show-where');        if (startBtn) startBtn.addEventListener('click', () => tryExecuteAutomation('pay saved beneficiary'));        if (highlightBtn) highlightBtn.addEventListener('click', () => {            try {                if (typeof sessionStorage !== 'undefined') {                    const pending = sessionStorage.getItem('pendingAutomation');                    if (pending) sessionStorage.removeItem('pendingAutomation');                }            } catch (_) {}            tryExecuteAutomation('pay saved beneficiary');        });        return;    }    if (/(once\s*-?\s*off|once off|one-?time|single.*payment|make a payment)/i.test(msg)) {        const steps = [            'Tap Transact at the bottom.',            'Select Payments.',            'Choose Once-off payment.',            'Enter recipient details, amount, and reference.',            'Review and confirm.'        ];        const html = `            <div>                <p>Here are the steps to make a once-off payment:</p>                <ol>${steps.map(s => `<li>${s}</li>`).join('')}</ol>                <div style="margin-top:8px;display:flex;gap:8px;flex-wrap:wrap;">                    <button id="btn-start-onceoff" style="padding:6px 10px;background:#007fff;color:#fff;border:none;border-radius:4px;cursor:pointer;">Start automation</button>                </div>            </div>        `;        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        const startBtn = li.querySelector('#btn-start-onceoff');        if (startBtn) startBtn.addEventListener('click', () => tryExecuteAutomation('make once off payment'));        return;    }    if (/((view|download).*(document|statement|invoice|proof)|documents)/i.test(msg)) {        const html = `            <div>                <p>To view or download your documents:</p>                <ol>                    <li>Go to Documents in the main menu.</li>                    <li>Choose the document type (e.g., Statements, Proof of payment).</li>                    <li>Select the account and period if prompted.</li>                    <li>Tap View to preview or Download to save/share.</li>                </ol>                <div style="margin-top:8px;display:flex;gap:8px;flex-wrap:wrap;">                    <button id="btn-start-docs" style="padding:6px 10px;background:#007fff;color:#fff;border:none;border-radius:4px;cursor:pointer;">Open documents</button>                </div>            </div>        `;        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        const startBtn = li.querySelector('#btn-start-docs');        if (startBtn) startBtn.addEventListener('click', () => tryExecuteAutomation('open documents'));        return;    }    if (/(balance|how much.*money|account info|account name)/i.test(msg)) {        const { name, balance } = getCurrentAccountInfo();        const li = createIncomingHtml(`<div class="chat-card"><div class="chat-title">Account balance</div><div class="chat-body">Your current account is <strong>${name}</strong> with a balance of <strong>${balance}</strong>.</div></div>`);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(fees?|charges|pricing|costs|monthly fee)/i.test(msg)) {        const html = buildCard({            title: 'Fees and pricing (Business)',            body: 'Capitec Business offers simple, transparent pricing designed for businesses. View our latest business banking fees online.',            links: [                { text: 'Capitec Business fees', href: 'https://www.capitecbank.co.za/business/' }            ],            chips: [                { label: 'Open Payments', value: 'open payments' },                { label: 'Open Cards', value: 'open cards' }            ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(global\s*one|global\s*biz|business account|account(s)?|types of account|open account)/i.test(msg)) {        const html = buildCard({            title: 'Capitec Business (Global Biz)',            body: 'Business banking built to simplify your operations: pay suppliers, manage cards and control limits with powerful digital tools.',            links: [                { text: 'Capitec Business overview', href: 'https://www.capitecbank.co.za/business/' }            ],            chips: [                { label: 'Open Accounts', value: 'go to account' },                { label: 'Open Products', value: 'open products and services' }            ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(immediate|instant|real\s*time|rtc).*pay/i.test(msg)) {        const html = buildCard({            title: 'Immediate payments (Business)',            body: 'Make immediate payments to suppliers or other banks. Fees and limits apply per business profile. For details, see Capitec Business.',            links: [                { text: 'Capitec Business payments', href: 'https://www.capitecbank.co.za/business/' }            ],            chips: [                { label: 'Make a payment', value: 'open payments' },                { label: 'Adjust limits', value: 'open settings' }            ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(cash\s*send|send cash|cashsend|pay supplier|pay suppliers|supplier payment)/i.test(msg)) {        const html = buildCard({            title: 'Business payments',            body: 'Pay suppliers or transfer between accounts. For cash-related services and other business tools, see Capitec Business.',            links: [                { text: 'Capitec Business payments', href: 'https://www.capitecbank.co.za/business/' }            ],            chips: [                { label: 'Open Payments', value: 'open payments' }            ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(limit(s)?|increase limit|card limit|daily limit|transaction limit)/i.test(msg)) {        const html = buildCard({            title: 'Card & transaction limits (Business)',            list: [                'Adjust card online/ATM limits',                'Set daily EFT/payment limits',                'Manage per-channel controls in Settings'            ],            links: [ { text: 'Capitec Business limits', href: 'https://www.capitecbank.co.za/business/' } ],            chips: [ { label: 'Open Settings', value: 'open settings' } ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(security|fraud|scam|safe|pin|otp)/i.test(msg)) {        const html = buildCard({            title: 'Security tips for business',            list: [                'Never share your PIN, OTP, or app login details',                'Verify payment requests and beneficiaries',                'Use the official app and keep your device updated',                'Report suspicious activity immediately'            ],            links: [ { text: 'Capitec Business security', href: 'https://www.capitecbank.co.za/business/' } ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(card|debit card|virtual card)/i.test(msg)) {        const html = buildCard({            title: 'Business cards',            list: [                'Business debit cards for everyday purchases',                'Virtual cards for safer online payments',                'Freeze/unfreeze and manage limits in-app'            ],            links: [ { text: 'Capitec Business cards', href: 'https://www.capitecbank.co.za/business/' } ],            chips: [ { label: 'Open Cards', value: 'open cards' } ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(credit|loan|personal loan|access facility)/i.test(msg)) {        const html = buildCard({            title: 'Business credit options',            body: 'Explore business-focused credit solutions (subject to approval). For more information, visit Capitec Business.',            links: [ { text: 'Capitec Business credit', href: 'https://www.capitecbank.co.za/business/' } ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(save|savings|fixed deposit|notice account)/i.test(msg)) {        const html = buildCard({            title: 'Business savings & tools',            list: [                'Flexible options to manage surplus business funds',                'Fixed-term options depending on availability',                'Set goals and track progress'            ],            links: [ { text: 'Capitec Business savings', href: 'https://www.capitecbank.co.za/business/' } ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    if (/(contact|call|support|client care|branch|hours|lost card)/i.test(msg)) {        const html = buildCard({            title: 'Contact Capitec Business',            body: 'Get business banking help via the app or online. For urgent issues like a lost or stolen card, block it in-app and contact support.',            links: [                { text: 'Capitec Business', href: 'https://www.capitecbank.co.za/business/' }            ]        });        const li = createIncomingHtml(html);        chatbox.appendChild(li);        chatbox.scrollTo(0, chatbox.scrollHeight);        return;    }    const suggestions = `        ${buildCard({            title: 'How can I help?',            body: 'Try one of these topics or ask your own question:',            list: [                'Pay a saved beneficiary',                'Make an immediate payment',                'View/download documents',                'Manage card and limits',                'Fees and pricing',                'Contact support'            ],            chips: [                { label: 'Open Payments', value: 'open payments' },                { label: 'Open Cards', value: 'open cards' },                { label: 'Open Settings', value: 'open settings' }            ]        })}    `;    const li = createIncomingHtml(suggestions);    chatbox.appendChild(li);    chatbox.scrollTo(0, chatbox.scrollHeight);};const handleChat = () => {    const text = chatInput.value.trim();    if (!text) return;    chatbox.appendChild(createChatLi(text, 'outgoing'));    chatInput.value = '';    chatbox.scrollTo(0, chatbox.scrollHeight);    setTimeout(() => {        const thinking = createChatLi('Thinking...', 'incoming');        chatbox.appendChild(thinking);        chatbox.scrollTo(0, chatbox.scrollHeight);        setTimeout(() => {            thinking.remove();            generateResponse(text);            logChatInteraction(text, extractPlainText(chatbox.lastElementChild));        }, 400);    }, 300);};if (sendChatBtn && chatInput && chatbox) {    sendChatBtn.addEventListener('click', handleChat);    chatInput.addEventListener('keydown', (e) => {        if (e.key === 'Enter' && !e.shiftKey) {            e.preventDefault();            handleChat();        }    });    chatbox.addEventListener('click', (e) => {        const target = e.target;        if (target && target.classList && target.classList.contains('chip')) {            const value = target.getAttribute('data-chip') || target.textContent || '';            const text = value.trim();            if (!text) return;            chatbox.appendChild(createChatLi(text, 'outgoing'));            chatbox.scrollTo(0, chatbox.scrollHeight);            if (/^(open |go to|make|pay|view)/i.test(text)) {                tryExecuteAutomation(text.toLowerCase());                logChatInteraction(text, '[Started automation]');                return;            }            setTimeout(() => {                generateResponse(text);                logChatInteraction(text, extractPlainText(chatbox.lastElementChild));            }, 150);        }    });}function extractPlainText(el) {    if (!el) return '';    var clone = el.cloneNode(true);    clone.querySelectorAll('.material-icons-sharp').forEach(function (icon) { icon.remove(); });    return clone.textContent.replace(/\s+/g, ' ').trim();}function logChatInteraction(question, answer) {    try {        var log = JSON.parse(localStorage.getItem('chatbotLog') || '[]');        log.push({ timestamp: new Date().toISOString(), question: question, answer: answer });        if (log.length > 500) log = log.slice(log.length - 500);        localStorage.setItem('chatbotLog', JSON.stringify(log));    } catch (e) {        console.error('Failed to log chat interaction:', e);    }}function csvEscape(value) {    var str = String(value === null || value === undefined ? '' : value);    if (/[",\n]/.test(str)) {        str = '"' + str.replace(/"/g, '""') + '"';    }    return str;}function exportChatLogToExcel() {    var log = [];    try {        log = JSON.parse(localStorage.getItem('chatbotLog') || '[]');    } catch (e) {        log = [];    }    if (!log.length) {        alert('No chat history to export yet. Ask BIZCHAT something first.');        return;    }    var rows = [['Timestamp', 'Question', 'Answer']].concat(        log.map(function (r) { return [r.timestamp, r.question, r.answer]; })    );    var csv = rows.map(function (row) { return row.map(csvEscape).join(','); }).join('\r\n');    var blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });    var url = URL.createObjectURL(blob);    var link = document.createElement('a');    link.href = url;    link.download = 'bizchat-history-' + new Date().toISOString().slice(0, 10) + '.csv';    document.body.appendChild(link);    link.click();    link.remove();    setTimeout(function () { URL.revokeObjectURL(url); }, 1000);}function addExportButton() {    var header = document.querySelector('.chatbot header');    if (!header || header.querySelector('.chat-export-btn')) return;    var btn = document.createElement('button');    btn.type = 'button';    btn.className = 'chat-export-btn';    btn.title = 'Export chat history to Excel (CSV)';    btn.innerHTML = '<span class="material-icons-sharp">download</span>';    btn.addEventListener('click', exportChatLogToExcel);    header.appendChild(btn);}addExportButton();function removeTeaserBubbles() {    var teaser = document.querySelector('.chatbot-teaser-bubbles');    if (teaser) teaser.remove();}var UPDATE_ANNOUNCEMENT_KEY = 'updateAnnouncementCreatedAt';var UPDATE_ANNOUNCEMENT_DISMISSED_KEY = 'updateAnnouncementDismissed';var UPDATE_ANNOUNCEMENT_LIFETIME_MS = 5 * 24 * 60 * 60 * 1000;var UPDATE_ANNOUNCEMENT_DISPLAY_MS = 15000;var UPDATE_ANNOUNCEMENT_ITEM_COUNT = 3;var UPDATE_CHANGELOG = [    { title: 'New Payments hub', desc: 'Pay saved or once-off beneficiaries, or manage groups, all in one place.', target: 'Phone%20Transact.html' },    { title: 'Redesigned Cards screen', desc: 'Card limits, stop card, and Apple Wallet in one refreshed view.', target: 'Phone-cards.html' },    { title: 'New Transfers hub', desc: 'See recent and upcoming transfers, or start a new one.', target: 'Phone%20Transact.html' },    { title: 'Karl Kumbier default beneficiary', desc: 'A protected default beneficiary is now always ready to pay.', target: 'Phone%20Transact.html' },    { title: 'Data Manager', desc: 'View and edit your accounts, transactions and beneficiaries directly.', target: 'Database.html' },    { title: 'Refreshed profile menu', desc: 'Switch profiles and sign out from the redesigned profile screen.', target: 'Profile-newlook.html' },    { title: 'GlobalOne personal banking refresh', desc: 'New icons and smoother navigation on Home, Cards, and Transact.', target: 'GlobalOne-Home.html' }];function resolveChangelogHref(target) {    if (window.location.pathname.indexOf('/Computer/') !== -1) return '../App/' + target;    return target;}function pickRandomChanges(count) {    var pool = UPDATE_CHANGELOG.slice();    for (var i = pool.length - 1; i > 0; i--) {        var j = Math.floor(Math.random() * (i + 1));        var tmp = pool[i]; pool[i] = pool[j]; pool[j] = tmp;    }    return pool.slice(0, count);}function injectUpdateAnnouncementStyles() {    if (document.getElementById('update-announcement-styles')) return;    var style = document.createElement('style');    style.id = 'update-announcement-styles';    style.textContent = '.update-announcement{position:fixed;top:20px;left:50%;' +        'display:flex;flex-direction:column;width:270px;background:#fff;color:#1f2430;' +        'border-radius:14px;box-shadow:0 14px 36px rgba(15,23,42,0.28);overflow:hidden;' +        'font-family:"Poppins",sans-serif;font-size:0.85rem;z-index:1003;opacity:0;' +        'animation:update-announcement-in 0.35s ease forwards;}' +        '.update-announcement-header{display:flex;align-items:center;gap:8px;background:#0070a0;' +        'color:#fff;padding:12px 14px;}' +        '.update-announcement-icon{font-size:1rem;flex-shrink:0;}' +        '.update-announcement-text{flex:1;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}' +        '.update-announcement-close{background:rgba(255,255,255,0.15);border:none;color:#fff;' +        'width:20px;height:20px;border-radius:50%;cursor:pointer;font-size:0.9rem;line-height:1;' +        'display:flex;align-items:center;justify-content:center;flex-shrink:0;padding:0;}' +        '.update-announcement-close:hover{background:rgba(255,255,255,0.28);}' +        '.update-announcement-list{display:flex;flex-direction:column;}' +        '.update-announcement-item{display:flex;align-items:center;gap:10px;padding:10px 14px;' +        'text-align:left;background:none;border:none;border-top:1px solid #edeff2;cursor:pointer;' +        'font-family:inherit;color:#1f2430;}' +        '.update-announcement-item:hover{background:#eaf4fa;}' +        '.update-announcement-item-body{flex:1;min-width:0;}' +        '.update-announcement-item-title{font-weight:600;font-size:0.82rem;color:#0f172a;}' +        '.update-announcement-item-desc{font-size:0.74rem;color:#6b7280;margin-top:2px;}' +        '.update-announcement-item-chevron{color:#0070a0;flex-shrink:0;font-size:0.95rem;}' +        '@keyframes update-announcement-in{from{opacity:0;transform:translateX(-50%) translateY(-12px);}' +        'to{opacity:1;transform:translateX(-50%) translateY(0);}}' +        '.update-announcement.update-announcement-fade-out{animation:update-announcement-out 0.4s ease forwards;}' +        '@keyframes update-announcement-out{to{opacity:0;transform:translateX(-50%) translateY(-12px);}}';    document.head.appendChild(style);}function showUpdateAnnouncement() {    var createdAtRaw = localStorage.getItem(UPDATE_ANNOUNCEMENT_KEY);    var createdAt;    if (!createdAtRaw) {        createdAt = Date.now();        localStorage.setItem(UPDATE_ANNOUNCEMENT_KEY, String(createdAt));    } else {        createdAt = Number(createdAtRaw);    }    if (Date.now() - createdAt >= UPDATE_ANNOUNCEMENT_LIFETIME_MS) {        localStorage.removeItem(UPDATE_ANNOUNCEMENT_KEY);        localStorage.removeItem(UPDATE_ANNOUNCEMENT_DISMISSED_KEY);        return;    }    if (localStorage.getItem(UPDATE_ANNOUNCEMENT_DISMISSED_KEY) === 'true') return;    if (document.querySelector('.update-announcement')) return;    injectUpdateAnnouncementStyles();    var changes = pickRandomChanges(UPDATE_ANNOUNCEMENT_ITEM_COUNT);    var el = document.createElement('div');    el.className = 'update-announcement';    el.innerHTML = '<div class="update-announcement-header">' +        '<span class="update-announcement-icon">\u2728</span>' +        '<span class="update-announcement-text">We\'ve made updates to the app</span>' +        '<button type="button" class="update-announcement-close" aria-label="Dismiss">&times;</button>' +        '</div>' +        '<div class="update-announcement-list">' +        changes.map(function (change, i) {            return '<button type="button" class="update-announcement-item" data-change-index="' + i + '">' +                '<span class="update-announcement-item-body">' +                '<span class="update-announcement-item-title">' + change.title + '</span>' +                '<span class="update-announcement-item-desc">' + change.desc + '</span>' +                '</span>' +                '<span class="update-announcement-item-chevron">\u203a</span>' +                '</button>';        }).join('') +        '</div>';    document.body.appendChild(el);    positionUpdateAnnouncement(el);    var autoFadeTimeout = setTimeout(function () { dismissUpdateAnnouncement(el, false); }, UPDATE_ANNOUNCEMENT_DISPLAY_MS);    el.querySelector('.update-announcement-close').addEventListener('click', function (e) {        e.stopPropagation();        clearTimeout(autoFadeTimeout);        dismissUpdateAnnouncement(el, true);    });    el.querySelectorAll('.update-announcement-item').forEach(function (item) {        item.addEventListener('click', function () {            var change = changes[Number(item.getAttribute('data-change-index'))];            clearTimeout(autoFadeTimeout);            localStorage.setItem(UPDATE_ANNOUNCEMENT_DISMISSED_KEY, 'true');            window.location.href = resolveChangelogHref(change.target);        });    });}function positionUpdateAnnouncement(el) {    var phone = document.querySelector('.iphone') || document.querySelector('.screen');    if (!phone) return;    var rect = phone.getBoundingClientRect();    el.style.left = (rect.left + rect.width / 2) + 'px';    el.style.top = (rect.top + 20) + 'px';}function dismissUpdateAnnouncement(el, permanent) {    if (permanent) localStorage.setItem(UPDATE_ANNOUNCEMENT_DISMISSED_KEY, 'true');    el.classList.add('update-announcement-fade-out');    setTimeout(function () {        if (el.parentNode) el.parentNode.removeChild(el);    }, 400);}function showTeaserBubbles() {    if (!chatbotToggler || !chatbox) return;    if (body && body.classList && body.classList.contains('show-chatbot')) return;    if (document.querySelector('.chatbot-teaser-bubbles')) return;    var topics = [        { icon: '👤', label: 'Pay a beneficiary', value: 'pay saved beneficiary' },        { icon: '💳', label: 'Card help', value: 'card' },        { icon: '📄', label: 'Documents', value: 'documents' }    ];    var container = document.createElement('div');    container.className = 'chatbot-teaser-bubbles';    container.innerHTML = topics.map(function (t, i) {        return '<button type="button" class="teaser-bubble" style="animation-delay:' + (i * 0.15) + 's" data-teaser="' + t.value + '">' + t.icon + ' ' + t.label + '</button>';    }).join('');    document.body.appendChild(container);    container.querySelectorAll('.teaser-bubble').forEach(function (btn) {        btn.addEventListener('click', function () {            var value = btn.getAttribute('data-teaser');            removeTeaserBubbles();            if (body && body.classList) body.classList.add('show-chatbot');            setTimeout(function () {                chatbox.appendChild(createChatLi(value, 'outgoing'));                chatbox.scrollTo(0, chatbox.scrollHeight);                setTimeout(function () { generateResponse(value); }, 150);            }, 150);        });    });    setTimeout(function () {        var el = document.querySelector('.chatbot-teaser-bubbles');        if (!el) return;        el.classList.add('teaser-fade-out');        setTimeout(removeTeaserBubbles, 500);    }, 6000);}if (chatbotToggler) {    chatbotToggler.addEventListener('click', () => {        removeTeaserBubbles();        if (body && body.classList) body.classList.toggle('show-chatbot');    });}if (closeBtn) {    closeBtn.addEventListener('click', () => {        if (body && body.classList) body.classList.remove('show-chatbot');    });}if (body && body.classList) body.classList.remove('show-chatbot');setTimeout(showTeaserBubbles, 1500);setTimeout(showUpdateAnnouncement, 800);